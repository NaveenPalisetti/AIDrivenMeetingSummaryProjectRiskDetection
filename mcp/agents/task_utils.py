import os, json

def extract_and_create_tasks(meeting_id, summary):
    # Dummy implementation: replace with your real logic
    # This should be the shared logic from TaskManagerAgent
    # For now, just return a list with a single dummy task
    return [{
        "meeting_id": meeting_id,
        "summary": summary,
        "task": "Dummy task created from summary"
    }]
